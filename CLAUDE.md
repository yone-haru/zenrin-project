<!-- 使い方: 【】を埋める → 不要なセクションは削除 → ★このコメントブロックは埋め終わったら削除（毎セッション読まれて文脈を消費するため）
     鉄則:
       - コードを読めば分かることは書かない。判断が変わる情報だけ書く
       - 増えてきたら外出し: 規約は .claude/rules/（paths: でパス限定ロード）／手順は .claude/skills/（発火時のみロード）
       - 注意: @import はトークンを減らさない（起動時にインライン展開される）。文脈の削減は rules / skills で行う -->

# 【プロジェクト名】

> 【1〜2文：何のプロジェクトで、誰が使い、何をするのか】

## 技術スタック
<!-- package.json 等の写経はしない。判断が変わる情報だけ（バージョン固定の理由・言語機能の可否など） -->
- 言語: 【例: TypeScript 5 strict / Python 3.12（match文可）】
- フレームワーク: 【例: Next.js / FastAPI / Expo SDK 52】
- DB: 【例: PostgreSQL / SQLite / Supabase】
- テスト: 【例: Vitest / pytest】
- デプロイ: 【例: Vercel / Oracle Cloud】

## コマンド
```bash
【install】   # 依存インストール
【dev】       # 開発サーバ起動
【test】      # テスト実行
【build】     # ビルド
【lint】      # Lint / 型チェック
```

## プロジェクト構成
<!-- 主要ディレクトリだけ。役割（なぜ存在するか）を一言添える。全部は書かない -->
```
src/
├── 【dir】/   # 【役割】
└── 【dir】/   # 【役割】
```

## ハマりどころ・既知の問題（随時追記・消さない）
<!-- コードを読んでも分からない部族知識をここに。ループ中に発見した罠も必ず追記する -->
- 【例: dev ビルドが4分かかる（アイコンの tree-shaking 由来・既知）。待てばOK】
- 【例: 認証メールは本番でしか飛ばない。dev では確認不可】
- 【例: `【test】` の◯◯テストは flaky。再実行で通る】

## 検証ループ（最重要・消さない）
- **IMPORTANT: コードを変更したら必ず `【test】` を実行し、全部通るまでコミットしない。**
- ビルド／型チェックも通すこと。エラーは握りつぶさず、根本原因を直す。
- コミットは論理的な区切りで分ける。大きなタスクも自律的にこなしてよい。
- 成功を主張する前に証拠を示す（テスト出力・実行コマンドと結果）。

## やってはいけないこと（境界・消さない）
<!-- ★秘密情報・本番操作など絶対に破られたくない項目は CLAUDE.md だけでは確実でない。
     PreToolUse フック or settings.json の permission deny で決定論的にも縛ること -->
- **YOU MUST NOT: `.env` や認証情報・APIキーをコミットしない。**
- スコープ外の大規模リファクタはしない。ただし根本原因の修正に必要な隣接コードは触ってよい。
- 【本番DB・本番環境を直接操作しない】
- 【自動生成ファイルを手で編集しない: 例 migrations、dist/、*.lock】
- 【破壊的・要注意コマンド: 例「`【build】` は dev サーバを壊すので実行しない」】

## エージェントの動き方（ループエンジニアリング・消さない）
<!-- 設計思想: 人がプロンプトを打つ役を外れ、Claude が自律的に完走する。
     「私の仕事はループを書くことだ」— Boris Cherny, Claude Code 責任者 -->
- **止まらない。途中で確認を求めない。** 仮定が必要なら合理的なベストを選び、何を仮定したか1行残して進む。
- ゴール未達・エラー発生いずれも止まらない。次の手を自分で決めて続行する（テスト修正・別アプローチ・追加調査など）。
- 数字・日付・仕様はファイルに根拠がない限り使わない。根拠がなければ「仮定:」と明記して進む。
- 複数ファイルにまたがる変更は、プランを先に `.claude/plan.md` へ書いてから実行する。
- ループ中に得た知見・進捗・試みたことは `.claude/loop-result.md` に随時記録する。
- **ループ中にプロジェクト固有の罠を発見したら、即座に「ハマりどころ」セクションに追記する。** 次のループが同じミスを繰り返さない。
- 実装と検証は分離する。実装後は別サブエージェント（Agent tool）に検証・レビューを依頼してよい。

### 停止条件（これ以外は止めない）
1. **完了**: 【完了条件: 例「全テスト・型チェック・Lint が通った」】
2. **試行上限**: 【N】ターン超えても前進なし → 原因と試みたことを `.claude/loop-result.md` に書いて停止

<!-- 作業中の状態（作業中 / ブロッカー / 次のステップ等）はここに書かない:
     すぐ陳腐化するうえ、Auto memory（v2.1.59+、デフォルト有効）が自動で記録する。
     継続中の作業は Issue やコミットに寄せる。
     どうしても個人メモを残すなら、gitignore したファイルを @import で読む:
       @.claude/local-notes.md   # ← .gitignore に追加しておく -->
